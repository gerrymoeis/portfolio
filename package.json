{
  "name": "gerrymoeis-portfolio",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run generate:cv && astro build",
    "build:dev": "npm run generate:cv && cross-env NODE_ENV=development DEPLOYMENT_ENVIRONMENT=development astro build",
    "build:staging": "npm run generate:cv && cross-env NODE_ENV=production DEPLOYMENT_ENVIRONMENT=staging astro build",
    "build:prod": "npm run generate:cv && cross-env NODE_ENV=production DEPLOYMENT_ENVIRONMENT=production astro build",
    "generate:cv": "node scripts/generate-cv.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "analyze": "npm run build && node scripts/analyze-bundles.js",
    "analyze:bundles": "node scripts/analyze-bundles.js"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^5.16.10",
    "glob": "^13.0.6",
    "gsap": "^3.14.2",
    "simple-icons": "^16.18.0"
  },
  "devDependencies": {
    "@vitest/ui": "^4.0.17",
    "cross-env": "^7.0.3",
    "fast-check": "^4.5.3",
    "vitest": "^4.0.17",
    "wrangler": "^4.59.3"
  }
}
