{
  "name": "gerrymoeis-portfolio",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/strip-html-comments.mjs",
    "build:dev": "cross-env NODE_ENV=development DEPLOYMENT_ENVIRONMENT=development astro build && node scripts/strip-html-comments.mjs",
    "build:staging": "cross-env NODE_ENV=production DEPLOYMENT_ENVIRONMENT=staging astro build && node scripts/strip-html-comments.mjs",
    "build:prod": "cross-env NODE_ENV=production DEPLOYMENT_ENVIRONMENT=production astro build && node scripts/strip-html-comments.mjs",
    "generate:cv": "node scripts/generate-cv.mjs",
    "generate:tech-icons": "node scripts/generate-tech-icons.mjs",
    "build:local": "npm run generate:tech-icons && npm run optimize-images && npm run generate:cv && npm run build",
    "optimize-images": "node scripts/optimize-images.mjs",
    "deploy": "node scripts/deploy.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "@fortawesome/fontawesome-free": "^7.3.0",
    "astro": "^5.16.10",
    "glob": "^13.0.6",
    "js-yaml": "^4.1.1",
    "sharp": "^0.34.5",
    "simple-icons": "^16.18.0",
    "tech-stack-icons": "^3.7.1"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vitest": "^4.0.17"
  }
}
