export interface BilingualText {
  en: string;
  id: string;
}

export interface ProjectLinks {
  github?: string;
  live?: string;
  demo?: string;
}

export interface ProjectLinkTitles {
  github?: string;
  live?: string;
  demo?: string;
}
