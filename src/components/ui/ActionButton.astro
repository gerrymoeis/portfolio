---
/**
 * ActionButton - Reusable action button component
 * Used for primary CTAs like "Download CV" and "Start Visualizer"
 * Features bilingual support and theme-aware styling
 * 
 * Props:
 * - href: Link URL (for <a> tag)
 * - onClick: Click handler (for <button> tag)
 * - icon: Icon name or SVG content
 * - labelEn: English label
 * - labelId: Indonesian label
 * - variant: 'fixed' (bottom-right) or 'inline' (in-flow)
 * - download: Boolean for download attribute
 * - className: Additional CSS classes
 * - ariaLabel: Accessibility label
 */

import Icon from './Icon.astro';

export interface Props {
  href?: string;
  icon?: string;
  iconSvg?: string;
  labelEn: string;
  labelId: string;
  variant?: 'fixed' | 'inline';
  download?: boolean;
  target?: string;
  className?: string;
  ariaLabel?: string;
  id?: string;
}

const { 
  href,
  icon,
  iconSvg,
  labelEn,
  labelId,
  variant = 'inline',
  download = false,
  target,
  className = '',
  ariaLabel,
  id
} = Astro.props;

const Tag = href ? 'a' : 'button';
const additionalProps = href 
  ? { href, ...(download && { download: true }), ...(target && { target, rel: 'noopener noreferrer' }) } 
  : { type: 'button' as const };
---

<Tag
  class={`action-button action-button--${variant} ${className}`}
  aria-label={ariaLabel || labelEn}
  data-bilingual="action-button"
  id={id}
  {...additionalProps}
>
  {icon && <Icon name={icon} class="action-button__icon" />}
  {iconSvg && <Fragment set:html={iconSvg} />}
  <span data-lang="en">{labelEn}</span>
  <span data-lang="id">{labelId}</span>
</Tag>

<style>
  .action-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-2);
    padding: var(--space-3) var(--space-6);
    font-family: var(--font-primary);
    font-size: var(--text-base);
    font-weight: var(--weight-semibold);
    text-decoration: none;
    border-radius: var(--radius-full);
    border: 2px solid transparent;
    cursor: pointer;
    transition: all var(--duration-medium) ease;
  }
  
  /* Inline variant - used in panels */
  .action-button--inline {
    position: relative;
  }
  
  /* Fixed variant - bottom LEFT corner (changed from right) */
  .action-button--fixed {
    position: fixed;
    bottom: calc(var(--space-12) + var(--space-4));
    left: var(--space-6);
    z-index: 1000;
  }
  

  
  .action-button__icon,
  .action-button svg {
    flex-shrink: 0;
    width: 18px;
    height: 18px;
    transition: all var(--duration-fast) var(--ease-out);
  }
  
  .action-button:active {
    transform: translateY(0);
  }
  
  .action-button:focus-visible {
    outline: 2px solid var(--color-accent-primary);
    outline-offset: 2px;
  }
  
  /* Dark theme styling */
  :global(html:not([data-theme="light"])) .action-button {
    background: var(--color-accent-primary);
    color: #0a0b0f;
    border-color: var(--color-accent-primary);
    box-shadow: 0 4px 12px rgba(var(--accent-rgb), 0.3);
  }
  
  :global(html:not([data-theme="light"])) .action-button:hover {
    background: var(--bg-elevated);
    color: var(--color-accent-primary);
    border-color: var(--color-accent-primary);
    box-shadow: 
      0 0 40px rgba(var(--accent-rgb), 0.8),
      0 0 20px rgba(var(--accent-rgb), 0.6),
      inset 0 0 20px rgba(var(--accent-rgb), 0.15);
    text-shadow: 0 0 10px rgba(var(--accent-rgb), 0.8);
    transform: translateY(-2px);
  }
  
  /* Light theme styling */
  :global(html[data-theme="light"]) .action-button {
    background: var(--color-accent-primary);
    color: #ffffff;
    border-color: var(--color-accent-primary);
    box-shadow: 0 4px 12px rgba(var(--accent-rgb), 0.3);
  }
  
  :global(html[data-theme="light"]) .action-button:hover {
    background: rgba(255, 255, 255, 1);
    color: var(--color-accent-primary);
    border-color: var(--color-accent-primary);
    box-shadow: 
      0 0 40px rgba(var(--accent-rgb), 0.8),
      0 0 20px rgba(var(--accent-rgb), 0.6),
      inset 0 0 20px rgba(var(--accent-rgb), 0.15);
    text-shadow: 0 0 10px rgba(var(--accent-rgb), 0.8);
    transform: translateY(-2px);
  }
  
  .action-button--fixed {
    animation: pulse-glow 2s ease-in-out infinite;
  }
  
  .action-button--fixed:hover {
    animation: none;
  }
  
  @keyframes pulse-glow {
    0%, 100% {
      box-shadow: 0 4px 12px rgba(var(--accent-rgb), 0.3);
    }
    50% {
      box-shadow: 0 4px 16px rgba(var(--accent-rgb), 0.5);
    }
  }
  
  /* Responsive - Mobile */
  @media (max-width: var(--breakpoint-xs)) {
    .action-button--inline {
      padding: var(--space-2) var(--space-4);
      font-size: var(--text-sm);
      gap: var(--space-1);
    }
    
    .action-button--fixed {
      bottom: calc(var(--space-4) + var(--space-4));
      left: var(--space-4);
      padding: var(--space-2) var(--space-3);
      font-size: var(--text-xs);
    }
    
    .action-button__icon,
    .action-button svg {
      width: 16px;
      height: 16px;
    }
  }
  
</style>
